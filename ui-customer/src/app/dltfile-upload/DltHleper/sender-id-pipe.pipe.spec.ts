import { SenderIdPipePipe } from './sender-id-pipe.pipe';

describe('SenderIdPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SenderIdPipePipe();
    expect(pipe).toBeTruthy();
  });
});
