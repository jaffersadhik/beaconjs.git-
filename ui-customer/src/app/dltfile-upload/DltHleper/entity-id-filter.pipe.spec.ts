import { EntityIdFilterPipe } from './entity-id-filter.pipe';

describe('EntityIdFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityIdFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
