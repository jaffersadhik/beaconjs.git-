import { BillingSearchPipe } from './billing-search.pipe';

describe('BillingSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BillingSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
