import { TwoWaySearchPipe } from './two-way-search.pipe';

describe('TwoWaySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TwoWaySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
